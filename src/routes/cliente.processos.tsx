import { createFileRoute } from "@tanstack/react-router";import { FileSearch } from "lucide-react";import { PageHeader } from "@/components/domain/page-header";import { formatDateTime } from "@/lib/domain/labels";import { processRepository } from "@/lib/repositories";
export const Route=createFileRoute("/cliente/processos")({head:()=>({meta:[{title:"Meus processos — Juris Agent"},{name:"description",content:"Acompanhe as últimas movimentações dos seus processos."},{property:"og:title",content:"Meus processos — Juris Agent"},{property:"og:description",content:"Acompanhe as últimas movimentações dos seus processos."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});function Page(){return <><PageHeader eyebrow="Acompanhamento" title="Processos" description="Últimas informações disponíveis no seu atendimento."/><div className="space-y-3">{processRepository.byClient("c-1").map(p=><article key={p.id} className="rounded-lg border border-border bg-surface p-5"><div className="flex items-start gap-3"><FileSearch className="mt-0.5 size-4 text-primary"/><div><p className="text-sm font-semibold">{p.cnj}</p><p className="mt-1 text-xs text-muted-foreground">{p.court}</p></div></div><div className="mt-5 border-t border-border pt-4"><p className="text-sm leading-6">{p.lastMovement}</p><p className="mt-2 text-xs text-muted-foreground">Atualizado em {formatDateTime(p.lastMovementAt)}</p></div></article>)}</div></>}
