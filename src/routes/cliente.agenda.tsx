import { createFileRoute } from "@tanstack/react-router";import { CalendarDays,MapPin } from "lucide-react";import { PageHeader } from "@/components/domain/page-header";import { formatFullDate,formatTime } from "@/lib/domain/labels";import { appointmentRepository } from "@/lib/repositories";
export const Route=createFileRoute("/cliente/agenda")({head:()=>({meta:[{title:"Minha agenda — Juris Agent"},{name:"description",content:"Seus próximos compromissos jurídicos."},{property:"og:title",content:"Minha agenda — Juris Agent"},{property:"og:description",content:"Seus próximos compromissos jurídicos."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});function Page(){return <><PageHeader eyebrow="Compromissos" title="Agenda" description="Audiências, reuniões e prazos relacionados ao seu caso."/><div className="space-y-3">{appointmentRepository.byClient("c-1").map(a=><article key={a.id} className="rounded-lg border border-border bg-surface p-5"><div className="flex gap-3"><CalendarDays className="size-4 text-primary"/><div><p className="font-display text-sm font-semibold">{a.title}</p><p className="mt-2 text-sm text-primary">{formatFullDate(a.startsAt)} · {formatTime(a.startsAt)}</p><p className="mt-2 flex items-center gap-2 text-xs text-muted-foreground"><MapPin className="size-3"/>{a.location}</p></div></div></article>)}</div></>}
