import { createFileRoute, Link } from "@tanstack/react-router"; import { useState } from "react"; import { ArrowRight, Search } from "lucide-react"; import { PageHeader } from "@/components/domain/page-header"; import { Input } from "@/components/ui/input"; import { clientRepository } from "@/lib/repositories"; import { clientStatusLabel, formatDate } from "@/lib/domain/labels";
export const Route=createFileRoute("/advogado/clientes")({head:()=>({meta:[{title:"Clientes — Juris Agent"},{name:"description",content:"Clientes, casos e próximos atendimentos."},{property:"og:title",content:"Clientes — Juris Agent"},{property:"og:description",content:"Clientes, casos e próximos atendimentos."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});
function Page(){const [q,setQ]=useState(""); const [filter,setFilter]=useState("all"); const all=clientRepository.list(); const rows=all.filter(c=>(filter==="all"||c.status===filter)&&c.name.toLowerCase().includes(q.toLowerCase())); const filters: [string,string][]=[["all","Todos"],["new","Novos"],["active","Ativos"],["waiting","Aguardando"],["pending","Pendências"]]; return <><PageHeader eyebrow="Relacionamentos" title="Clientes" description="Visão unificada dos atendimentos e próximos passos."/><div className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-1 overflow-x-auto">{filters.map(([k,l])=><button key={k} onClick={()=>setFilter(k)} className={`shrink-0 rounded-md px-3 py-2 text-xs ${filter===k?"bg-accent text-foreground":"text-muted-foreground hover:text-foreground"}`}>{l}</button>)}</div><div className="relative w-full sm:w-64"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"/><Input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar cliente" className="pl-9"/></div></div><div className="overflow-x-auto rounded-lg border border-border"><table className="w-full min-w-[780px] text-left text-sm"><thead className="border-b border-border bg-surface"><tr className="text-xs text-muted-foreground"><th className="px-4 py-3 font-medium">Cliente</th><th className="px-4 py-3 font-medium">Status</th><th className="px-4 py-3 font-medium">Casos</th><th className="px-4 py-3 font-medium">Último contato</th><th className="px-4 py-3 font-medium">Próxima atividade</th><th/></tr></thead><tbody className="divide-y divide-border">{rows.map(c=><tr key={c.id} className="bg-background hover:bg-surface"><td className="px-4 py-4"><p className="font-medium">{c.name}</p><p className="mt-1 text-xs text-muted-foreground">{c.phone}</p></td><td className="px-4"><span className="rounded-md border border-border bg-surface px-2 py-1 text-xs">{clientStatusLabel[c.status]}</span></td><td className="px-4 text-muted-foreground">{c.caseCount}</td><td className="px-4 text-muted-foreground">{formatDate(c.lastContactAt)}</td><td className="max-w-56 px-4 text-muted-foreground">{c.nextActivity}</td><td className="px-4"><Link to="/advogado/clientes/$id" params={{id:c.id}} className="flex size-8 items-center justify-center rounded-md text-muted-foreground hover:bg-accent hover:text-foreground"><ArrowRight className="size-4"/></Link></td></tr>)}</tbody></table></div></>}
