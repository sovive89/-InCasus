import { createFileRoute,useNavigate } from "@tanstack/react-router";import { LogOut,Mail,Phone,ShieldCheck,UserRound } from "lucide-react";import { PageHeader } from "@/components/domain/page-header";import { Button } from "@/components/ui/button";import { profiles } from "@/lib/mock/data";import { signOut } from "@/lib/auth/session";
export const Route=createFileRoute("/cliente/perfil")({head:()=>({meta:[{title:"Meu perfil — InCasus"},{name:"description",content:"Dados da sua conta e privacidade."},{property:"og:title",content:"Meu perfil — InCasus"},{property:"og:description",content:"Dados da sua conta e privacidade."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});function Page(){const p=profiles.find(x=>x.role==="client");const nav=useNavigate();return <><PageHeader eyebrow="Sua conta" title="Perfil"/><div className="rounded-lg border border-border bg-surface p-5"><div className="flex items-center gap-4 border-b border-border pb-5"><div className="flex size-12 items-center justify-center rounded-md bg-primary/10 text-primary"><UserRound/></div><div><h2 className="font-display font-semibold">{p?.name}</h2><p className="mt-1 text-xs text-muted-foreground">Cliente</p></div></div><div className="space-y-4 py-5 text-sm text-muted-foreground"><p className="flex items-center gap-3"><Mail className="size-4 text-primary"/>{p?.email}</p><p className="flex items-center gap-3"><Phone className="size-4 text-primary"/>{p?.phone}</p><p className="flex items-start gap-3"><ShieldCheck className="mt-0.5 size-4 text-primary"/><span>Seus dados são isolados e acessíveis apenas a você e aos profissionais autorizados.</span></p></div><Button variant="outline" onClick={()=>{signOut();nav({to:"/login"})}}><LogOut/>Sair</Button></div></>}
