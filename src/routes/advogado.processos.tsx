import { createFileRoute,Link } from "@tanstack/react-router"; import { ArrowRight } from "lucide-react"; import { PageHeader } from "@/components/domain/page-header"; import { StatusBadge } from "@/components/domain/status-badge"; import { clientRepository,processRepository } from "@/lib/repositories"; import { formatDate } from "@/lib/domain/labels";
export const Route=createFileRoute("/advogado/processos")({head:()=>({meta:[{title:"Processos — Juris Agent"},{name:"description",content:"Processos acompanhados e níveis de atenção."},{property:"og:title",content:"Processos — Juris Agent"},{property:"og:description",content:"Processos acompanhados e níveis de atenção."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});
function Page(){return <><PageHeader eyebrow="Monitoramento" title="Processos" description="Movimentações, consultas e pontos de atenção."/><div className="overflow-x-auto rounded-lg border border-border"><table className="w-full min-w-[900px] text-left"><thead className="border-b border-border bg-surface text-xs text-muted-foreground"><tr><th className="px-4 py-3 font-medium">Processo</th><th className="px-4 py-3 font-medium">Cliente</th><th className="px-4 py-3 font-medium">Última movimentação</th><th className="px-4 py-3 font-medium">Consulta</th><th className="px-4 py-3 font-medium">Atenção</th><th/></tr></thead><tbody className="divide-y divide-border text-sm">{processRepository.list().map(p=><tr key={p.id} className="hover:bg-surface"><td className="px-4 py-4"><p className="font-medium">{p.cnj}</p><p className="mt-1 text-xs text-muted-foreground">{p.court}</p></td><td className="px-4">{clientRepository.byId(p.clientId)?.name}</td><td className="max-w-sm px-4 text-muted-foreground"><p>{p.lastMovement}</p><p className="mt-1 text-xs">{formatDate(p.lastMovementAt)}</p></td><td className="px-4 text-muted-foreground">{formatDate(p.lastCheckedAt)}</td><td className="px-4"><StatusBadge level={p.attention}/></td><td className="px-4"><Link to="/advogado/processos/$id" params={{id:p.id}} className="flex size-8 items-center justify-center rounded-md text-muted-foreground hover:bg-accent hover:text-foreground"><ArrowRight className="size-4"/></Link></td></tr>)}</tbody></table></div></>}
