import { createFileRoute } from "@tanstack/react-router";import { FileText,Upload } from "lucide-react";import { PageHeader } from "@/components/domain/page-header";import { Button } from "@/components/ui/button";import { analysisStatusLabel,documentCategoryLabel,formatDate } from "@/lib/domain/labels";import { documentRepository } from "@/lib/repositories";
export const Route=createFileRoute("/cliente/documentos")({head:()=>({meta:[{title:"Meus documentos — Juris Agent"},{name:"description",content:"Documentos vinculados ao seu atendimento."},{property:"og:title",content:"Meus documentos — Juris Agent"},{property:"og:description",content:"Documentos vinculados ao seu atendimento."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});function Page(){return <><PageHeader eyebrow="Arquivos" title="Documentos" description="Consulte os documentos do seu atendimento." action={<Button disabled variant="outline"><Upload/>Enviar documento</Button>}/><div className="space-y-2">{documentRepository.byClient("c-1").map(d=><div key={d.id} className="flex items-start gap-3 rounded-lg border border-border bg-surface p-4"><FileText className="mt-0.5 size-4 shrink-0 text-primary"/><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{d.name}</p><p className="mt-1 text-xs text-muted-foreground">{documentCategoryLabel[d.category]} · {formatDate(d.createdAt)}</p><p className="mt-2 text-[10px] text-primary">{analysisStatusLabel[d.analysisStatus]}</p></div></div>)}</div><p className="mt-4 text-[10px] text-muted-foreground">O envio de documentos será habilitado após a conexão do armazenamento seguro.</p></>}
