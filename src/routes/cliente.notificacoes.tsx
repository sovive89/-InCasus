import { createFileRoute } from "@tanstack/react-router";import { Bell } from "lucide-react";import { PageHeader } from "@/components/domain/page-header";import { StatusBadge } from "@/components/domain/status-badge";import { formatDateTime } from "@/lib/domain/labels";import { notificationRepository } from "@/lib/repositories";
export const Route=createFileRoute("/cliente/notificacoes")({head:()=>({meta:[{title:"Notificações — Juris Agent"},{name:"description",content:"Atualizações do seu atendimento jurídico."},{property:"og:title",content:"Notificações — Juris Agent"},{property:"og:description",content:"Atualizações do seu atendimento jurídico."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});function Page(){return <><PageHeader eyebrow="Atualizações" title="Notificações" description="Acompanhe as novidades do seu atendimento."/><div className="divide-y divide-border rounded-lg border border-border">{notificationRepository.byAudience("client").map(n=><div key={n.id} className={`flex gap-3 p-4 ${n.state==="unread"?"bg-surface":""}`}><Bell className="mt-0.5 size-4 shrink-0 text-primary"/><div><div className="flex flex-wrap items-center gap-2"><p className="text-sm font-semibold">{n.title}</p><StatusBadge level={n.level}/></div><p className="mt-1 text-sm text-muted-foreground">{n.description}</p><p className="mt-2 text-[10px] text-muted-foreground">{formatDateTime(n.createdAt)}</p></div></div>)}</div></>}
