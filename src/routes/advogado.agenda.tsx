import { createFileRoute } from "@tanstack/react-router"; import { CalendarDays,Clock,MapPin } from "lucide-react"; import { PageHeader } from "@/components/domain/page-header"; import { appointmentRepository,clientRepository } from "@/lib/repositories"; import { formatFullDate,formatTime } from "@/lib/domain/labels";
export const Route=createFileRoute("/advogado/agenda")({head:()=>({meta:[{title:"Agenda — Juris Agent"},{name:"description",content:"Compromissos, audiências e prazos do escritório."},{property:"og:title",content:"Agenda — Juris Agent"},{property:"og:description",content:"Compromissos, audiências e prazos do escritório."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page}); function Page(){const items=appointmentRepository.list();return <><PageHeader eyebrow="Planejamento" title="Agenda" description="Audiências, reuniões, chamadas e prazos."/><div className="space-y-3">{items.map(a=><article key={a.id} className="grid gap-4 rounded-lg border border-border bg-surface p-5 sm:grid-cols-[170px_1fr]"><div><p className="text-xs text-muted-foreground">{formatFullDate(a.startsAt)}</p><p className="mt-1 flex items-center gap-2 font-display text-xl font-semibold text-primary"><Clock className="size-4"/>{formatTime(a.startsAt)}</p></div><div><h2 className="font-display text-base font-semibold">{a.title}</h2><p className="mt-2 text-sm text-muted-foreground">{clientRepository.byId(a.clientId)?.name}</p><p className="mt-2 flex items-center gap-2 text-xs text-muted-foreground"><MapPin className="size-3"/>{a.location}</p></div></article>)}</div></>}
